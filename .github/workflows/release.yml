name: Build & Release BM Player

on:
  push:
    tags:
      - 'v*.*.*'
  workflow_dispatch:

jobs:
  # ── x64 Build ────────────────────────────────────────────
  build-x64:
    name: Windows x64 Installer
    runs-on: windows-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci --ignore-scripts

      - name: Generate icon
        run: node scripts/generate-icon.js

      - name: Convert icon PNG to ICO
        shell: pwsh
        run: |
          $pngPath = "buildResources\icon.png"
          $icoPath = "buildResources\icon.ico"
          Add-Type -AssemblyName System.Drawing
          $bmp = [System.Drawing.Bitmap]::FromFile((Resolve-Path $pngPath).Path)
          $sizes = @(16, 32, 48, 64, 128, 256)
          $stream = [System.IO.File]::Create($icoPath)
          $writer = New-Object System.IO.BinaryWriter($stream)
          $writer.Write([uint16]0); $writer.Write([uint16]1); $writer.Write([uint16]$sizes.Count)
          $offset = 6 + $sizes.Count * 16; $images = @()
          foreach ($size in $sizes) {
            $resized = New-Object System.Drawing.Bitmap($bmp, $size, $size)
            $ms = New-Object System.IO.MemoryStream
            $resized.Save($ms, [System.Drawing.Imaging.ImageFormat]::Png)
            $images += ,$ms.ToArray(); $resized.Dispose()
          }
          foreach ($i in 0..($sizes.Count-1)) {
            $sz=$sizes[$i]; $data=$images[$i]
            $writer.Write([byte]($sz -band 0xFF)); $writer.Write([byte]($sz -band 0xFF))
            $writer.Write([byte]0); $writer.Write([byte]0)
            $writer.Write([uint16]1); $writer.Write([uint16]32)
            $writer.Write([uint32]$data.Length); $writer.Write([uint32]$offset); $offset+=$data.Length
          }
          foreach ($data in $images) { $writer.Write($data) }
          $writer.Close(); $stream.Close(); $bmp.Dispose()

      - name: Download and Extract mpv x64
        shell: pwsh
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          # Create the exact directory expected by electron-builder.yml
          New-Item -ItemType Directory -Force -Path "vendor\mpv"
          Write-Host "Fetching latest mpv x64 release using wildcard matching..."
          $releases = gh api repos/shinchiro/mpv-winbuild-cmake/releases/latest | ConvertFrom-Json
          
          # Bulletproof wildcard match
          $asset = $releases.assets | Where-Object { $_.name -like "mpv-x86_64-*.7z" -and $_.name -notlike "*debug*" -and $_.name -notlike "*dev*" } | Select-Object -First 1
          
          if (-not $asset) {
              Write-Error "Could not find a matching mpv release asset! Found assets:"
              $releases.assets.name
              exit 1
          }
          
          Write-Host "Found asset: $($asset.name). Downloading..."
          gh release download -R shinchiro/mpv-winbuild-cmake -p $asset.name
          
          Write-Host "Extracting directly to vendor\mpv directory..."
          # Extract to the correct vendor directory
          7z e $asset.name -o"vendor\mpv" -y
          Remove-Item $asset.name -Force

      - name: Build x64 installer
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: npx electron-builder --win nsis --x64 --publish always

      - name: Upload x64 artifact
        uses: actions/upload-artifact@v4
        with:
          name: BM-Player-x64
          path: dist/BM-Player-Setup-*-x64.exe
          if-no-files-found: warn

  # ── x86 Build ────────────────────────────────────────────
  build-x86:
    name: Windows x86 Installer
    runs-on: windows-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci --ignore-scripts

      - name: Generate icon
        run: node scripts/generate-icon.js

      - name: Convert icon PNG to ICO
        shell: pwsh
        run: |
          $pngPath = "buildResources\icon.png"
          $icoPath = "buildResources\icon.ico"
          Add-Type -AssemblyName System.Drawing
          $bmp = [System.Drawing.Bitmap]::FromFile((Resolve-Path $pngPath).Path)
          $sizes = @(16, 32, 48, 64, 128, 256)
          $stream = [System.IO.File]::Create($icoPath)
          $writer = New-Object System.IO.BinaryWriter($stream)
          $writer.Write([uint16]0); $writer.Write([uint16]1); $writer.Write([uint16]$sizes.Count)
          $offset = 6 + $sizes.Count * 16; $images = @()
          foreach ($size in $sizes) {
            $resized = New-Object System.Drawing.Bitmap($bmp, $size, $size)
            $ms = New-Object System.IO.MemoryStream
            $resized.Save($ms, [System.Drawing.Imaging.ImageFormat]::Png)
            $images += ,$ms.ToArray(); $resized.Dispose()
          }
          foreach ($i in 0..($sizes.Count-1)) {
            $sz=$sizes[$i]; $data=$images[$i]
            $writer.Write([byte]($sz -band 0xFF)); $writer.Write([byte]($sz -band 0xFF))
            $writer.Write([byte]0); $writer.Write([byte]0)
            $writer.Write([uint16]1); $writer.Write([uint16]32)
            $writer.Write([uint32]$data.Length); $writer.Write([uint32]$offset); $offset+=$data.Length
          }
          foreach ($data in $images) { $writer.Write($data) }
          $writer.Close(); $stream.Close(); $bmp.Dispose()

      - name: Download and Extract mpv x86
        shell: pwsh
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          # Create the exact directory expected by electron-builder.yml
          New-Item -ItemType Directory -Force -Path "vendor\mpv"
          Write-Host "Fetching latest mpv i686 release using wildcard matching..."
          $releases = gh api repos/shinchiro/mpv-winbuild-cmake/releases/latest | ConvertFrom-Json
          
          # Bulletproof wildcard match for 32-bit version
          $asset = $releases.assets | Where-Object { $_.name -like "mpv-i686-*.7z" -and $_.name -notlike "*debug*" -and $_.name -notlike "*dev*" } | Select-Object -First 1
          
          if (-not $asset) {
              Write-Error "Could not find a matching mpv x86 release asset! Found assets:"
              $releases.assets.name
              exit 1
          }
          
          Write-Host "Found asset: $($asset.name). Downloading..."
          gh release download -R shinchiro/mpv-winbuild-cmake -p $asset.name
          
          Write-Host "Extracting directly to vendor\mpv directory..."
          # Extract to the correct vendor directory
          7z e $asset.name -o"vendor\mpv" -y
          Remove-Item $asset.name -Force

      - name: Build x86 installer
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: npx electron-builder --win nsis --ia32 --publish always

      - name: Upload x86 artifact
        uses: actions/upload-artifact@v4
        with:
          name: BM-Player-x86
          path: dist/BM-Player-Setup-*-ia32.exe
          if-no-files-found: warn